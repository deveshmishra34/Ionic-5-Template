import { ImageCacheDirective } from './image-cache.directive';

describe('ImageCacheDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageCacheDirective();
    expect(directive).toBeTruthy();
  });
});
